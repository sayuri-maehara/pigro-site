# Pigro株式会社 コーポレートサイト

このファイルは Claude Code が毎回読み込む前提ルールです。作業前に必ず全体を確認してください。

## プロジェクト概要

福岡市中央区大名にある2名体制の会社のコーポレートサイト。1ページ完結型（LP形式）。
静的HTML + Vercel Functions。フレームワーク・ビルドツールは使っていません。

**想定読者**：福岡の中小企業経営者（40代以上）。**チラシや紹介で社名を知り、「信頼できる会社か」を確かめに来る人が主**。
検索流入は副次的。すでに「何かしてくれそう」と思って来ているので、悩みを煽るより「誰が・どこで・何を」を先に見せる。
一部、求職者個人も来訪する（主役にはしないが、LINE への入口を用意する）。

**サイトの唯一の目的**：お問い合わせ（電話・LINE・フォーム）を増やすこと。信頼確認で来た人を帰さず、連絡まで導く。

## ファイル構成

```
index.html      トップページ（CSS・JSともにインライン）
privacy.html    プライバシーポリシー
api/contact.js  フォーム送信処理（Vercel Functions + Resend）
package.json    "type": "module" 指定。ESM動作に必要なので消さないこと
assets/         画像一式
```

---

## 変更してはいけないもの

以下は、指示がない限り絶対に変更しないでください。「ついでに改善」も禁止です。

### 1. 配色トークン

`index.html` 冒頭の `:root` で定義。**値を変えない。新しい色を勝手に追加しない。**

| 変数 | 色 | 用途 |
|---|---|---|
| `--navy` | `#14213D` | 文字色、ボタン、中間CTA、ロゴ |
| `--navy-deep` | `#0C1629` | フッター |
| `--paper` | `#FAF8F3` | ページ全体の地色 |
| `--surface` | `#FFFFFF` | 白いセクション、カード |
| `--sand` | `#EDE5D5` | ベージュの面 |
| `--sand-2` | `#DED2B9` | 下線、罫線、装飾 |
| `--accent` | `#8A6C3A` | 小さなラベル文字のみ |

**設計上の制約（守ること）**
- 濃紺を大面積に敷かない。地は常に `--paper` か `--surface` を保つ。濃紺はヘッダー・フッター・中間CTAのみ
- ベージュは面と装飾専用。本文の文字色に使わない（読みづらくなる）
- グラデーション、影の多用、丸みの強い角丸は使わない。`border-radius` は 2px 基調

### 2. 書体

- 見出し：`Zen Old Mincho`（明朝）
- 本文：`Zen Kaku Gothic New`
- 英数字ラベル・番号：`Jost`

この3つ以外を読み込まないこと。

### 3. 法定表記・会社情報

以下は法令および登記に基づく情報です。**言い換え・要約・省略をしないこと。**

- 有料職業紹介事業　厚生労働大臣許可 **40-ユ-301648号**
- 電気通信媒介業　届出番号 **J2500301**
- 社名：Pigro株式会社（ピグロ）／設立：2023年11月／資本金：900万円
- 代表者：代表取締役　安武 伸浩
- 所在地：〒810-0041 福岡県福岡市中央区大名2丁目4-38-404
- 電話：092-401-3675（平日 9:00–18:00）／メール：pigro@pigro-fukuoka.com

### 4. 掲載しない事業

旧サイトに記載があるが、実態と異なるため**復活させないこと**。

- 営業代行業
- コールセンター業務
- アウトソーシング事業の受託及び請負

### 5. 構造上の決定事項

過去に検討して却下した案です。提案し直さないでください。

- **ニュース欄は作らない**（更新が止まると逆効果になるため）
- **スライダー（自動切替の複数画像）は使わない**（2枚目以降が見られない、CTAが押し下げられる）
- **ストック写真は使わない**（テンプレート然として見え、信頼を損なう）
- **「私たちの想い」を単独セクションにしない**（抽象的で読み飛ばされるため代表紹介と統合済み）
- **下層ページを増やさない**（2名体制では更新が追いつかない。privacy.html のみ例外）
- **「こんなお悩み、ありませんか」を独立セクションにしない**（2026-09-15 決定。紹介で来た人には遠回り。事業内容の冒頭1行に統合）
- **ヒーローに代表の顔写真を置かない**（2026-09-16 代表の意向。「最初に自分の顔が出るのは嫌」。ヒーロー右側は線のモチーフ＋「余白をつくり、一点に力を集める。」。写真は代表紹介に260pxで）
- **理念・ミッション・ビジョンの3点セットを置かない**（2026-09-15 決定。どの会社でも言える言葉は読み飛ばされる。理念「整えてから、攻める」1行のみ）
- **対応エリアを地域名のタグで並べない**（2026-09-15 決定。線引きに見える。会社概要の表に「訪問：九州・山口／オンライン：全国」の1行）
- **中間CTAで3事業を列挙しない**（2026-09-15 決定。「何でも屋」に見える。共通価値「整える」を言う）
- **博多ロータリークラブの所属は載せない**（2026-09-18 代表判断。ロゴも文章も不可。営業利用と見られるのを避けるため。提案し直さない）
- **求職者向けの独立セクションは作らない**（2026-09-15）。ただし代表の意向（2026-09-16）で、人材・採用の段の中に「お仕事をお探しの方へ」ブロック（ベージュ面・LINEボタン付き）を置く。求職者にも刺さる文面にすること

---

## セクション構成（2026-09-15 改定）

上から順に。順番を変えないこと。「信頼確認で来た人が知りたい順」＝ ①誰・どこ・何 → ②何をしてくれるか → ③この人に頼めるか → ④どう頼むか。

1. ヘッダー（ロゴ／ナビ5項目：事業内容・代表紹介・ご相談の流れ・会社概要・お問い合わせ／電話＋受付時間／無料相談ボタン）
2. ヒーロー（左：キャッチ「整えてから、攻める。」＋一文「福岡で、経営・採用・通信の相談に乗る会社」／右：線のモチーフ（アニメーション）＋キャプション）
3. 事業内容（冒頭に悩み1行。3本柱を**横長の段として縦に積む**（左：柱名＋番号／右：内容。2026-09-16 変更。横3列カードは文量差で不揃いになるため廃止。人材を主役にする2段組Dは代表判断で検討可）。「こんな方に」は廃止（代表の意向）。経営コンサルはカタカナ多めのコンサル調で抽象度高く（代表の意向）。人材・採用の末尾に求職者向け LINE 入口）
4. 代表紹介（左：代表写真260px／右：経歴・資格・休日の表、社名の由来＋ロゴ、理念1行）
5. 中間CTA（濃紺。見出し「削るもの、足すもの。まず一度、整理しませんか。」）
6. ご相談の流れ（01〜04の番号付き）
7. 会社概要（表＋Googleマップ。表に「対応範囲：訪問は福岡県内・九州・山口／オンラインは全国」の行）
8. お問い合わせ（上：電話・LINE の大ボタン2つが主役／下：フォーム4項目＝お名前・連絡先・内容・同意／求職者向け LINE 入口）
9. フッター
10. スマホ下部固定バー（電話／LINE／無料相談）

**各セクションの文字量の原則**：数十秒で判断する人は段落を読まない。見出し＋箇条書きで伝える。
- 事業内容の各柱は「見出し＋こんな方に（1行）＋できること（箇条書き3点以内）＋許可・届出番号」の型に統一。段落文は置かない
- 代表紹介は「20年超」「資格2つ」など具体的で本人にしか言えない事実を前に出す

**番号の使い分け**：ご相談の流れは実際の順序なので 01〜04 を振る。
事業内容の3本柱は順序ではないので、番号を主役にしない（`01 Cost` 等は補助ラベル）。

**代表写真**：`assets/yasutake.jpg`（明るい・背景すっきり・やわらかい表情の3条件を満たすと確認済み）。
**代表紹介の1か所だけ**に使う（幅260px）。ヒーローには置かない（代表の意向）。

**会社ロゴ**：`assets/logo.jpg`（ナマケモノ。132×133px・白背景 JPG）。ヘッダーと、代表紹介の「社名の由来」の横に使用。
白背景は CSS の `mix-blend-mode:multiply` で地に溶かしている（濃い背景には使えない。フッターは文字のまま）。
小さい画像なので 120px より大きく表示しない。大きく使いたい場合は元データ（ベクター or 高解像度）を入手すること。

**画像パス**：`assets/…`（先頭に `/` を付けない）。`index.html` をダブルクリックで直接開いても表示されるようにするため。

**人材・採用の文章（2026-09-16 代表確認済み）**：媒体名・データサービス名（ジョバディ、トレマチ等）は**載せない**。
「約15万件の**求人**データ（2026年9月時点）」は代表確認済みの数字（**求職者データの件数ではない**。2026-09-16 訂正）。景表法対応として、提携先の求人件数が分かる画面・自社分・計算メモを**手元に保管**しておくこと（サイトには載せない）。数字や時点を更新するときは代表に再確認。
求職者からは手数料を取っていない（代表確認済み）ので「登録・相談・紹介まで無料」と書いてよい。
年齢層「20〜30代中心」、職種「製造・物流・建設・飲食・介護〜事務職」は実態と合っていると代表確認済み。
**「福岡」は会社の所在地としてだけ書く**（ヒーロー・会社概要）。人材・採用の文中で「福岡の企業に強い」等のサービス範囲としては書かない（2026-09-16。求人・求職者とも全国対応のため、地域限定に読まれるのを避ける）。

**代表の税理士試験**：載せない（代表の意向、2026-09-16）。登録もしていないため「税理士」表記は不可。「税務相談」等をサービスとして書かない。
資格欄は本人の資格のみ（有料職業紹介の許可は会社への許可なので載せない）。

---

## 品質の下限（毎回維持すること）

- **読む文章は 16px 以上**（スマホも16px）。補助ラベル・注記は最小 12px。読者が40〜60代のため、14px台の本文は「小さい」と指摘済み（2026-09-16 代表）。新しく文章を足すときは `.biz-lead`／`.biz-can` 等の既存クラスを使い、独自に小さくしない
- スマホ（375px幅）で崩れない
- キーボード操作時にフォーカスリングが見える
- `prefers-reduced-motion` を尊重（アニメーションを止める）
- JavaScript が無効でも全セクションが表示される（`.js` クラスでスコープ済み）
- 画像に `alt` と `loading="lazy"`
- 外部リンクに `rel="noopener"`

## 触るときの注意

- **CSSセレクタの詳細度**：`.sect` と `.midcta` のように、要素ベースと種別ベースのクラスが
  余白を打ち消し合いやすい。padding / margin を追加するときは既存の指定を確認すること
- **フォームの `name` 属性**：`form.name` は HTMLFormElement 自身の `name` を指すため、
  必ず `form.elements['name']` でアクセスする（過去にバグの原因になった箇所）
- **`api/contact.js` の送信元**：`MAIL_FROM` に `pigro@pigro-fukuoka.com` を使わない。
  SPF/DKIM の検証に失敗して迷惑メール判定されるため、`noreply@` を使い Reply-To に問い合わせ者を入れる

---

## 作業の進め方

1. 変更前に「どのファイルのどの範囲を触るか」を先に述べること
2. **依頼されていない箇所を変更しない**。改善案がある場合は実装せず、提案として述べるだけにする
3. 1回のやりとりで1セクションまで。複数セクションの同時変更はしない
4. 変更後、影響範囲（他セクションへの波及）を報告すること

## 改修タスク（2026-09-15 決定 → 2026-09-16 完了）

- [x] ヒーロー：線のアニメーション廃止 → 代表写真＋名前。左に一文「福岡・大名で、経営・採用・通信の相談に乗る会社」
- [x] 「こんなお悩み」セクション削除 → 事業内容の冒頭に1行統合
- [x] 事業内容：各柱を「見出し＋こんな方に＋できること3点＋許可番号」の型へ。人材・採用の末尾に求職者向け LINE 入口
- [x] 中間CTA：代表紹介の後へ移動。見出しを「削るもの、足すもの。まず一度、整理しませんか。」に
- [x] 代表紹介：経歴・資格を箇条書き化。理念・ミッション・ビジョン → 理念1行のみ。趣味1行
- [x] 会社概要：対応エリアのタグ廃止 → 表に「対応範囲」の行を追加
- [x] お問い合わせ：電話・LINE の大ボタンを上に。フォームを4項目（お名前・メールアドレス・内容・同意）に縮小。`api/contact.js` も合わせた。LINE カードが求職者入口を兼ねる
- [x] 全体：スマホ375px・フォーカスリング・reduced-motion・noopener の確認

**補足**：フォームの連絡先は「メールか電話」ではなく**メールアドレス固定**にした（自動返信と Reply-To に必須のため。電話派は上の電話ボタンを使う）。
ヒーロー写真だけは `loading="lazy"` を付けず `fetchpriority="high"`（最初の画面の画像は遅延読み込みすると逆に遅くなる）。

## 公開方針（2026-09-16 決定）

- **Vercel 無料プラン（Hobby）で公開する**。GitHub は `sayuri-maehara/pigro-site`。公開URL: https://pigro-site.vercel.app/ （2026-09-16 初回デプロイ。独自ドメイン接続は未）
- 無料プランは規約上「個人・非営利向け」と承知のうえで使用。Vercel から有料化の連絡が来たら、
  **Cloudflare Pages（0円・商用可）＋ Web3Forms（フォーム）に移行する**（README の方式B）。
  移行時の作業：Cloudflare 連携／`index.html` の `ENDPOINT` を Web3Forms に変更／`api/` 削除／お名前.com の DNS 変更／プライバシーポリシーに外部サービス利用の一文追記
- 月額費用は現状0円。既存のドメイン更新費（お名前.com、年1,500〜2,000円程度）のみ継続

## 未完了のタスク（改修前からの持ち越し）

- [ ] Resend のドメイン認証（SPF/DKIM を お名前.com の DNS に登録）
- [ ] 地図の表示位置が正しいか確認（旧サイトの埋め込みURLを流用中）
- [ ] 公式LINE URL `https://lin.ee/iLw0kzs` の有効性確認
- [ ] 届出番号 J2500301 を届出受理通知の原本と照合
- [ ] 人材サービス総合サイトへの掲載有無を確認（未掲載ならフッターのリンクを外す）
- [ ] 写真の追加（撮影後。優先順位は README.md 参照）
- [ ] プライバシーポリシーの専門家チェック
