# Pigro株式会社 コーポレートサイト

このファイルは Claude Code が毎回読み込む前提ルールです。作業前に必ず全体を確認してください。

## プロジェクト概要

福岡市中央区大名にある2名体制の会社のコーポレートサイト。1ページ完結型（LP形式）。
静的HTML + Vercel Functions。フレームワーク・ビルドツールは使っていません。

**想定読者**：福岡の中小企業経営者（50〜60代が中心）。コスト削減や採用に困って検索・紹介で流入し、
「信頼できそうか」「何をしてくれる会社か」を数十秒で判断する。

**サイトの唯一の目的**：お問い合わせ（フォーム・電話・LINE）を増やすこと。

## ファイル構成

```
index.html      トップページ（CSS・JSともにインライン）
privacy.html    プライバシーポリシー
api/contact.js  フォーム送信処理（Vercel Functions + Resend）
package.json    "type": "module" 指定。ESM動作に必要なので消さないこと
assets/         画像一式
```

---

## 変更してはいけないもの

以下は、指示がない限り絶対に変更しないでください。「ついでに改善」も禁止です。

### 1. 配色トークン

`index.html` 冒頭の `:root` で定義。**値を変えない。新しい色を勝手に追加しない。**

| 変数 | 色 | 用途 |
|---|---|---|
| `--navy` | `#14213D` | 文字色、ボタン、中間CTA、ロゴ |
| `--navy-deep` | `#0C1629` | フッター |
| `--paper` | `#FAF8F3` | ページ全体の地色 |
| `--surface` | `#FFFFFF` | 白いセクション、カード |
| `--sand` | `#EDE5D5` | ベージュの面 |
| `--sand-2` | `#DED2B9` | 下線、罫線、装飾 |
| `--accent` | `#8A6C3A` | 小さなラベル文字のみ |

**設計上の制約（守ること）**
- 濃紺を大面積に敷かない。地は常に `--paper` か `--surface` を保つ。濃紺はヘッダー・フッター・中間CTAのみ
- ベージュは面と装飾専用。本文の文字色に使わない（読みづらくなる）
- グラデーション、影の多用、丸みの強い角丸は使わない。`border-radius` は 2px 基調

### 2. 書体

- 見出し：`Zen Old Mincho`（明朝）
- 本文：`Zen Kaku Gothic New`
- 英数字ラベル・番号：`Jost`

この3つ以外を読み込まないこと。

### 3. 法定表記・会社情報

以下は法令および登記に基づく情報です。**言い換え・要約・省略をしないこと。**

- 有料職業紹介事業　厚生労働大臣許可 **40-ユ-301648号**
- 電気通信媒介業　届出番号 **J2500301**
- 社名：Pigro株式会社（ピグロ）／設立：2023年11月／資本金：900万円
- 代表者：代表取締役　安武 伸浩
- 所在地：〒810-0041 福岡県福岡市中央区大名2丁目4-38-404
- 電話：092-401-3675（平日 9:00–18:00）／メール：pigro@pigro-fukuoka.com

### 4. 掲載しない事業

旧サイトに記載があるが、実態と異なるため**復活させないこと**。

- 営業代行業
- コールセンター業務
- アウトソーシング事業の受託及び請負

### 5. 構造上の決定事項

過去に検討して却下した案です。提案し直さないでください。

- **ニュース欄は作らない**（更新が止まると逆効果になるため）
- **スライダー（自動切替の複数画像）は使わない**（2枚目以降が見られない、CTAが押し下げられる）
- **ストック写真は使わない**（テンプレート然として見え、信頼を損なう）
- **「私たちの想い」を単独セクションにしない**（抽象的で読み飛ばされるため代表紹介と統合済み）
- **下層ページを増やさない**（2名体制では更新が追いつかない。privacy.html のみ例外）

---

## セクション構成

上から順に。順番を変えないこと。

1. ヘッダー（ロゴ／ナビ5項目／電話＋受付時間／無料相談ボタン）
2. ヒーロー（キャッチ「整えてから、攻める。」／整列アニメーション）
3. こんなお悩み、ありませんか
4. 事業内容（3本柱：経営・コスト／人材・採用／通信・決済）
5. 中間CTA（濃紺のセクション）
6. 代表紹介＋想い＋社名の由来（統合セクション）
7. ご相談の流れ（01〜04の番号付き）
8. 会社概要＋対応エリア＋Googleマップ
9. お問い合わせフォーム＋電話・LINE・メール
10. フッター
11. スマホ下部固定バー（電話／LINE／無料相談）

**番号の使い分け**：ご相談の流れは実際の順序なので 01〜04 を振る。
事業内容の3本柱は順序ではないので、番号を主役にしない（`01 Cost` 等は補助ラベル）。

---

## 品質の下限（毎回維持すること）

- スマホ（375px幅）で崩れない
- キーボード操作時にフォーカスリングが見える
- `prefers-reduced-motion` を尊重（アニメーションを止める）
- JavaScript が無効でも全セクションが表示される（`.js` クラスでスコープ済み）
- 画像に `alt` と `loading="lazy"`
- 外部リンクに `rel="noopener"`

## 触るときの注意

- **CSSセレクタの詳細度**：`.sect` と `.midcta` のように、要素ベースと種別ベースのクラスが
  余白を打ち消し合いやすい。padding / margin を追加するときは既存の指定を確認すること
- **フォームの `name` 属性**：`form.name` は HTMLFormElement 自身の `name` を指すため、
  必ず `form.elements['name']` でアクセスする（過去にバグの原因になった箇所）
- **`api/contact.js` の送信元**：`MAIL_FROM` に `pigro@pigro-fukuoka.com` を使わない。
  SPF/DKIM の検証に失敗して迷惑メール判定されるため、`noreply@` を使い Reply-To に問い合わせ者を入れる

---

## 作業の進め方

1. 変更前に「どのファイルのどの範囲を触るか」を先に述べること
2. **依頼されていない箇所を変更しない**。改善案がある場合は実装せず、提案として述べるだけにする
3. 1回のやりとりで1セクションまで。複数セクションの同時変更はしない
4. 変更後、影響範囲（他セクションへの波及）を報告すること

## 未完了のタスク

- [ ] Resend のドメイン認証（SPF/DKIM を お名前.com の DNS に登録）
- [ ] 地図の表示位置が正しいか確認（旧サイトの埋め込みURLを流用中）
- [ ] 公式LINE URL `https://lin.ee/iLw0kzs` の有効性確認
- [ ] 届出番号 J2500301 を届出受理通知の原本と照合
- [ ] 人材サービス総合サイトへの掲載有無を確認（未掲載ならフッターのリンクを外す）
- [ ] 写真の追加（撮影後。優先順位は README.md 参照）
- [ ] プライバシーポリシーの専門家チェック
